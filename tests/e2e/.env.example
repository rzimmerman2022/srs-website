# Playwright E2E Test Environment Variables
# Copy this file to .env.local and fill in the values

# Base URL for testing (default: http://localhost:3000)
PLAYWRIGHT_TEST_BASE_URL=http://localhost:3000

# Optional: Test credentials for successful login tests
# These are used in the skipped tests that verify successful authentication
TEST_ADMIN_EMAIL=admin@example.com
TEST_ADMIN_PASSWORD=your-test-password

# Optional: Valid questionnaire token for testing successful access
# This should be a real token from your test database
TEST_QUESTIONNAIRE_TOKEN=valid-test-token-from-database

# CI environment flag (automatically set by CI/CD systems)
# CI=true
